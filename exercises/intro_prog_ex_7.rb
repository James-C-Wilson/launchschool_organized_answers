# What's the difference between a Hash and an Array?

# An array is an ordered list of elements that can be of any type.
# A hash is a data structure that stores items by associated keys. 

# Arrays use an index; hashes use keys.